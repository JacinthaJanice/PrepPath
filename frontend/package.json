{
  "name": "preppath-frontend",
  "version": "3.0.0",
  "description": "PrepPath study planner frontend",
  "scripts": {
    "dev": "npx live-server --port=3000 --open=index.html",
    "serve": "npx serve . -p 3000",
    "lint": "npx htmlhint index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.1"
  }
}
